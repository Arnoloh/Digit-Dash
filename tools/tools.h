#include <stdio.h>
#include <stdlib.h>
#include <string.h>
// Structure chat
typedef struct chat
{
    int id;
    char *message;
} chat;
// Structure info_game
typedef struct info_game
{
    int id;
    char info;
    char Hello;
} info_game;
#define MAX_STRING_LENGTH 1024
